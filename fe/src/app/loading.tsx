import { PageLoading } from "@/components/ui/loading";

export default function Loading() {
    return (
        <PageLoading
            title="Đang tải..."
            description="Chúng tôi đang chuẩn bị nội dung cho bạn"
        />
    );
}