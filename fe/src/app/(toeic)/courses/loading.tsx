import { PageLoading } from "@/components/ui/loading";

export default function CoursesLoading() {
    return (
        <PageLoading
            title="Đang tải khóa học..."
            description="Chúng tôi đang chuẩn bị danh sách khóa học dành cho bạn"
        />
    );
}