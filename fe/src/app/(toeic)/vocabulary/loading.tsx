import { PageLoading } from "@/components/ui/loading";

export default function VocabularyLoading() {
    return (
        <PageLoading
            title="Đang tải từ vựng..."
            description="Chúng tôi đang chuẩn bị bộ từ vựng phù hợp với trình độ của bạn"
        />
    );
}